(function (global) {
	var app = global.app = global.app || {},
		data = app.data = app.data || {};
    
    // NOT Implemented and not used for now.
    app.loginHelper = {
      'login': function() {
          
      }  
    };
    
})(window)